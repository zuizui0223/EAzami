# EAzami current state

Status date: 2026-09-01

## Active status

Scientific status: **SPACE_BREADTH_TIME_DEPTH_REFRAME_ACTIVE**  
Submission status: **V5_QA_COMPLETE_BUT_PRE_REFRAME — V6_REBUILD_REQUIRED**

The active Chapter 2 frame is no longer the V5 environment-first synthesis. V5 remains a reproducible production snapshot, but the scientific mainline is being rebuilt around two independent dimensions:

```text
SPACE — present geographic breadth / spatial structure
TIME  — evolutionary depth / recurrence / branch distribution
```

Active contract:

`data/evidence/chapter2_space_breadth_time_depth_contract_v1.json`

Active manuscript architecture:

`docs/chapter2/MANUSCRIPT_JEB_V6_REFRAME_OUTLINE.md`

## Current scientific objective

> **Describe how broadly capitulum traits are distributed in present geographic space, how deeply and repeatedly those same trait modules are assembled through evolutionary time, and then synthesize the two axes without forcing environmental causation or one whole-head syndrome.**

Trait × environment screens are now internal diagnostics only. They may remain absent from the final Results.

## SPACE axis — current pilot

Environment-free public-geography pilot:

- workflow `33480007180`;
- artifact `9789577849`;
- 23 authority-backed taxa considered; 19 have at least three source-name-guarded, spatially thinned public occurrences;
- orientation: 17 singleton-state taxa; U/D state breadths are ~1000 km scale and state segregation is unsupported (P=0.370);
- stickiness: 12 singleton-state taxa; both states span ~800–1000 km and the segregation tendency is unsupported (P=0.131);
- phyllary posture: only four singleton-state taxa pass the spatial gate, so the comparable spatial inference is limited.

Detailed result:

`docs/chapter2/SPACE_BREADTH_TIME_DEPTH_PILOT_RESULT_V1.md`

## TIME axis — frozen result

### Orientation

- 20 resolved concepts;
- ML minimum changes = 6;
- UFBoot minimum = 4–6, median 5;
- median relative lineage-depth envelope = 0.795–0.994;
- fraction of UFBoot trees requiring at least one internal change in every minimum reconstruction = 0.628.

### Phyllary posture

- ten resolved concepts under the temporal ontology, including constrained ambiguous state sets;
- exactly 3 minimum changes across ML and all 1,000 UFBoot trees;
- median relative-depth envelope = 0.695–1.000.

### Stickiness

- 13 resolved concepts;
- exactly 5 minimum changes across ML and all 1,000 UFBoot trees;
- median relative-depth envelope = 0.937–0.954.

Zero of three discrete trait pairs passes the robust shared-transition-localization rule.

## SPACE × TIME feasibility

Workflow `33480348070` joined the environment-free spatial pilot to the frozen temporal-depth result without constructing a composite score.

Current diagnostic reading:

- orientation and stickiness are both geographically broad at the current taxon-state level;
- orientation permits a broader deep–shallow temporal-history envelope;
- stickiness carries the same broad current geography with a much shallower relative-depth profile;
- phyllary has good temporal information but insufficient primary spatial singleton-state coverage.

This establishes only that the two axes are empirically separable. No regression is fitted across three modules.

## Internal exploratory diagnostics

The 7×4 and 7×9 trait × environment screens and their topology sensitivity remain reproducible project diagnostics. They are useful for generating or falsifying process ideas, but:

- they do not define spatial breadth;
- they do not select manuscript traits;
- they do not need to appear in the paper;
- unsupported or unstable rows stay diagnostic rather than being converted into claims.

## Historical process examples

Historical environment/range work is supporting, not the chapter organizer.

Retain where independently identifiable:

- orientation chronology × palaeolocation envelope: 94 age pairs × 4 regions = 376 scenarios; historical direction unresolved;
- dated white–coloured sister-system phenotype contrasts;
- repeated coarse circularity/solidity/visible-floret remodelling directions.

These examples do not define the SPACE axis.

## V5 production snapshot

`docs/chapter2/MANUSCRIPT_JEB_V5.md` remains a fully QA'd pre-reframe snapshot:

- abstract 220 words;
- 4,980 main-text words before references;
- five embedded main figures;
- 26/26 rendered pages visually reviewed;
- accessibility high/medium/low = 0/0/0;
- anonymous DOCX metadata/comments/tracked changes checked.

Those production results remain valid, but V5 is **not the active submission text** after the current scientific reframe.

Working V6 title direction:

> **Spatial breadth and evolutionary depth of capitulum diversity in an East-Asian thistle radiation**

## Next analytical priority

1. expand/validate the primary SPACE estimands beyond the discrete pilot where public phenotype geography permits;
2. preserve trait-specific coverage and `not_evaluable` rather than forcing one common metric;
3. join the final spatial evidence registry to the frozen temporal-depth registry;
4. rebuild the manuscript only after the two-axis evidence table is stable;
5. keep environmental and historical-process analyses as secondary explanations unless independently justified.

## Claim boundary

Chapter 2 does not establish adaptation, selection, convergence, exact transition ages, historical range from current occurrences, common environmental cause, or a universal synchronized capitulum syndrome.

## Legacy downstream doctoral routing labels retained for compatibility

These exact phrases are historical routing aliases and are not the active Chapter 2 framing:

- `Chapter 1 — phenotype × present-day space/environment`;
- `Chapter 2 — phenotype × evolutionary time/history`;
- `Chapter 3 — own RAD-seq × linked phenotype/function`;
- `origin discrimination`;
- `nuclear population genomics`;
- `plastid haplotype`;
- `cytotype`;
- `FDT1 trait-to-function evidence`;
- `Cirsium reproductive-herbivory RR = 2.674`;
- `Chapter 3 causal layer`.

## Legacy JEB V3/V4 audit aliases retained for validator compatibility

- `HOLD_JEB_PACKAGE_REBUILD_ONLY`;
- `Active standalone title`;
- `v4 is current submission text`;
- `CHAPTER2_CORE_RESULT_RECOVERY_V1.md`.

These labels preserve historical audit routing only.
