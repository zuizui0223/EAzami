name: Pilot takaoense targeted DFR ANS SRA-BLAST

on:
  pull_request:
    paths:
      - "analysis/extract_cnipponicum_flavonoid_candidate_proteins.py"
      - "analysis/summarize_targeted_sra_vdb_hits.py"
      - "data/evidence/cnipponicum_flavonoid_family_validation_v2.json"
      - "data/evidence/cirsium_nipponicum_public_genome_comp1061_contract_v1.json"
      - ".github/workflows/pilot-takaoense-targeted-sra-vdb.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pilot:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install pinned NCBI BLAST+ 2.17.0 with VDB executables
        run: |
          cd /tmp
          URL='https://ftp.ncbi.nlm.nih.gov/blast/executables/blast+/2.17.0/ncbi-blast-2.17.0+-x64-linux.tar.gz'
          curl -fsSLO "$URL"
          curl -fsSLO "$URL.md5"
          md5sum -c ncbi-blast-2.17.0+-x64-linux.tar.gz.md5
          tar -xzf ncbi-blast-2.17.0+-x64-linux.tar.gz
          echo "/tmp/ncbi-blast-2.17.0+/bin" >> "$GITHUB_PATH"
          /tmp/ncbi-blast-2.17.0+/bin/tblastn_vdb -version
      - name: Download frozen C. nipponicum proteome and extract DFR ANS queries
        run: |
          python - <<'PY'
          import hashlib,json,pathlib,urllib.request
          c=json.load(open('data/evidence/cirsium_nipponicum_public_genome_comp1061_contract_v1.json'))
          x=c['public_files']['protein']; out='/tmp/C.nipponicum_aa.fa'
          urllib.request.urlretrieve(x['download_url'],out)
          md5=hashlib.md5(pathlib.Path(out).read_bytes()).hexdigest()
          if md5 != x['md5']: raise SystemExit(f'MD5 mismatch: {md5}')
          print(pathlib.Path(out).stat().st_size,md5)
          PY
          python -m py_compile analysis/extract_cnipponicum_flavonoid_candidate_proteins.py analysis/summarize_targeted_sra_vdb_hits.py
          python analysis/extract_cnipponicum_flavonoid_candidate_proteins.py \
            --proteome /tmp/C.nipponicum_aa.fa \
            --output /tmp/cnip_dfr_ans.faa \
            --manifest /tmp/cnip_dfr_ans_query_manifest.json
      - name: Search smallest morph-linked takaoense SRA run directly
        run: |
          tblastn_vdb \
            -query /tmp/cnip_dfr_ans.faa \
            -db SRR35152735 \
            -evalue 1e-5 \
            -max_target_seqs 5000 \
            -seg yes \
            -outfmt '6 qseqid sseqid pident length qlen slen qstart qend sstart send evalue bitscore' \
            -out /tmp/NH_ccy3835_dfr_ans_vdb.tsv
          wc -l /tmp/NH_ccy3835_dfr_ans_vdb.tsv
      - name: Summarize assay-level detectability
        run: |
          python analysis/summarize_targeted_sra_vdb_hits.py \
            --hits /tmp/NH_ccy3835_dfr_ans_vdb.tsv \
            --run SRR35152735 \
            --sample-id NH_ccy3835 \
            --morph BP \
            --output /tmp/NH_ccy3835_dfr_ans_vdb_summary.json
          python - <<'PY'
          import json
          d=json.load(open('/tmp/NH_ccy3835_dfr_ans_vdb_summary.json'))
          assert d['contract_version']=='targeted_takaoense_sra_vdb_screen_v1'
          assert d['run']=='SRR35152735' and d['sample_id']=='NH_ccy3835' and d['morph']=='BP'
          assert set(d['queries'])=={'CNIP_DFR','CNIP_ANS'}
          assert 'not genomic absence' in d['claim_boundary'].lower()
          print(json.dumps(d,indent=2,ensure_ascii=False))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: takaoense-targeted-dfr-ans-sra-vdb-${{ github.run_id }}
          path: |
            /tmp/cnip_dfr_ans.faa
            /tmp/cnip_dfr_ans_query_manifest.json
            /tmp/NH_ccy3835_dfr_ans_vdb.tsv
            /tmp/NH_ccy3835_dfr_ans_vdb_summary.json
          if-no-files-found: error
          retention-days: 90
